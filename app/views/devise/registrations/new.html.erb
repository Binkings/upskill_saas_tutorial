<div class="container">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <div class= "text-center">
        <% if params[:plan] == '2' %>
          <h1>Pro Account</h1>
          <p>Schrijf je in voor een Pro account zodat je wat meer info kan zien. Kost je slechts €10 per maand! Koopje!</p>
        <% else %>
          <h1>Basic account</h1>
          <p>Schrijf je in voor een basic account. Kost je niks, maar je krijgt ook niks.</p>
        <% end %>
      </div>
      
      <div class="well">
        <h2>Sign up</h2>
        <% if params[:plan] == '2'%>
          <%= render "pro_form" %>
        <% else %>
          <%= render "basic_form" %>
        <% end %>
        
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>